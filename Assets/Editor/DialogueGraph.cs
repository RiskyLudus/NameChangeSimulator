using UnityEngine;
using XNode;

[CreateAssetMenu(fileName = "DialogueGraph", menuName = "Graphs/Dialogue Graph")]
public class DialogueGraph : NodeGraph
{
    // The graph doesn't need additional properties unless you want to add global variables or settings
}